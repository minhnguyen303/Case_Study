class Fuel {
    constructor(img, x, y, point) {
        this.img = img;
        this.x = x;
        this.y = y;
        this.point = point;
    }

    setPos(x, y){
        this.x = x;
        this.y = y;
    }
}